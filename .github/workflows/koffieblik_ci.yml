name: CI for Koffieblik Login (Local Demo)

on:
  push:
    branches: [main]
    paths:
      - 'app/**'
      - '.github/workflows/nextjs-ci.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./app

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Lint code
        run: npm run lint

      - name: Build the app
        run: npm run build
